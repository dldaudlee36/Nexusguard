# GIGANG 코드 주석본

전체 소스에 한국어 주석을 추가한 버전입니다.

> **2026-09-10 갱신** — 아래 `⚠` 로 표시돼 있던 문제들을 실제로 고쳤습니다.
> 따라서 이제 **실행 코드도 바뀌었습니다.** 바뀐 곳에는 `[수정됨]` / `[추가됨]` 주석을 달아
> 무엇이 왜 바뀌었는지 원래 코드와 함께 설명해 두었습니다.
> 변경된 파일과 내용은 이 문서 맨 아래 "수정 내역" 절에 정리했습니다.

## 확인 방법

```bash
python3 run_demo.py      # 엔진이 정상 동작하는지 확인 (인시던트 5건, 미승인 자산 7건)
```

`_주석검증.py` 는 "주석만 추가되고 실행 코드는 동일한가"를 대조하는 스크립트입니다.
이제 실행 코드를 의도적으로 고쳤으므로 이 검증은 더 이상 통과하지 않습니다.

## 주석을 단 파일 (13개, 5,419줄 → 6,973줄)

### 읽는 순서 추천

처음 보는 사람은 이 순서로 읽으면 전체가 이해됩니다.

| 순서 | 파일 | 무엇을 알 수 있나 |
|---|---|---|
| 1 | `nexusguard/schemas/event.py` | 로그가 어떤 형태로 다뤄지는지 |
| 2 | `nexusguard/schemas/incident.py` | 분석 결과가 어떤 형태인지 |
| 3 | **`nexusguard/engine/correlation.py`** | **판정 로직 전부 (여기가 핵심)** |
| 4 | `nexusguard/storage/sqlite_store.py` | 상태와 이력을 어떻게 보관하는지 |
| 5 | `nexusguard/engine/respond.py` | 2단계 알림이 어떻게 갈리는지 |
| 6 | `source/agent/agent.py` | 단말에서 로그를 어떻게 모으는지 |
| 7 | `nexusguard/collectors/team_collector.py` | 받은 로그를 어떻게 변환하는지 |
| 8 | `nexusguard/ui/app.py` | 화면이 어떻게 그려지는지 |

### 전체 목록

```
app.py                              Streamlit 진입점
run_demo.py                         터미널 점검 스크립트
nexusguard/
  __init__.py  ...  각 패키지 __init__.py 6개에 역할 설명 추가
  schemas/event.py                  로그 표준 형식
  schemas/incident.py               분석 결과 형식
  engine/correlation.py             ★ 위험 상태 판정 엔진
  engine/governance.py              섀도우 AI 자산 관리
  engine/respond.py                 알림 훅
  storage/sqlite_store.py           영속 저장
  storage/memory_store.py           엔진 초기화 (싱글톤)
  collectors/team_collector.py      로그 수집·정규화
  generators/dummy_logs.py          시연용 가짜 로그
  ui/app.py                         대시보드 화면
source/agent/agent.py               Windows Agent
source/railway_server/app.py        중앙 수집 서버
browser_extension/content.js        크롬 확장
browser_extension/README.md         (신규) manifest.json 설명 — JSON은 주석 불가
```

## 주석에 함께 표시한 것

동작 설명 외에, 코드를 읽다 막힐 만한 지점에 `⚠` 표시로 메모를 남겼습니다.
그중 고친 것은 `✅ 수정됨`, 아직 남아 있는 것은 `⚠ 남아 있음` 으로 구분했습니다.

| 위치 | 내용 | 상태 |
|---|---|---|
| `storage/memory_store.py` 상단 | 실제 수집 로그가 엔진으로 들어가지 않는 이유 (DNS 조건 불일치) | ✅ 수정됨 |
| `engine/correlation.py` `ingest_events` | 이 입구로 더미 로그만 들어오고 있음 | ✅ 수정됨 |
| `sqlite_store.has_user_prior_watch_history` | 기간 제한 없이 전체 이력을 조회 (요건은 24시간) | ✅ 수정됨 |
| `railway_server` `raw_data` | `str(data)`로 저장해 JSON이 아니게 되는 문제 | ✅ 수정됨 |
| `browser_extension/content.js` 상단 | 클립보드 붙여넣기 미감지 | ✅ 수정됨 |
| `collectors/team_collector.py` 상단 | `activity.log` 경로가 특정 PC에 고정되어 DB_SELECT 이벤트가 생기지 않음 | ⚠ 남아 있음 |
| `engine/respond.py` 상단 | 슬랙 알림이 로그 출력만 하고 실제 발송은 안 됨 / 자동 차단은 의도적으로 꺼둔 것 | ⚠ 남아 있음 |
| `storage/sqlite_store.py` 상단 | TTL 만료가 조회 필터로만 동작해 자가 치유 이력이 안 남음 | ⚠ 남아 있음 |
| `source/agent/agent.py` 상단 | 모든 도메인을 수집해 개인정보 문제 소지 | ⚠ 남아 있음 |
| `source/agent/agent.py` 하단 | Agent 시작 전 방문한 사이트는 로그가 안 올라옴 (시연 시 주의) | ⚠ 남아 있음 |
| `source/railway_server/app.py` 상단 | `POST /events`와 `/logs`에 인증 없음, API 키 기본값이 코드에 있음 | ⚠ 남아 있음 |
| `browser_extension/content.js` 상단 | 드래그앤드롭 미감지 | ⚠ 남아 있음 |
| `ui/app.py` 상단 | 화면에 보이는 WATCH/HIGH가 대부분 시뮬레이션 결과임 | ⚠ 남아 있음 |

---

# 수정 내역 (2026-09-10)

## 두 버전 병합 (2026-09-10)

주석본은 `GIGANG-main-fixed` 보다 오래된 버전이었습니다.
fixed 쪽에만 있던 신규 기능을 주석본으로 가져오면서 **주석은 전부 살렸습니다.**

| 파일 | 가져온 기능 |
|---|---|
| `engine/governance.py` | `sync_railway_events()` — 실제 수집 로그로 자산의 접속 건수·사용자·사용 빈도 갱신, 신규 도메인 자동 등록 |
| | `get_railway_domain_stats()`, `get_connection_count()` |
| | `analyze_domain_with_gemini(live_context=...)` — LLM에 실제 사용 현황을 함께 넘김 |
| `schemas/incident.py` | `ShadowAIAsset.access_count`, `active_users` 필드 |
| `collectors/team_collector.py` | `get_railway_api_key()` — 앱 실행 중에 키를 넣어도 반영되도록 매번 다시 읽음 |
| `ui/app.py` | 거버넌스 화면이 카드 나열 → 열 헤더 클릭 정렬 표로 변경, 실시간 접속 열 추가, 인증 키 상태 표시 |

주석 수: 주석본 원본 199개 → 병합 후 238개 (유실 0, 신규 기능 설명 추가)

`ui/app.py` 는 주석본 쪽을 기준으로 두고 fixed 의 코드 변경만 옮겨 심는 방식으로 병합했습니다.
그래서 기존 주석이 한 줄도 사라지지 않았습니다.

## 고친 파일 8개

| 파일 | 내용 |
|---|---|
| `nexusguard/engine/correlation.py` | 판정 순회를 timestamp 오름차순으로 / 하드코딩 15분을 `self.time_window` 로 통일 / 인시던트 ID를 겹치지 않게 / 중복 이벤트 차단 / 붙여넣기 판정 추가 |
| `nexusguard/storage/sqlite_store.py` | `has_user_prior_watch_history` 에 최근 24시간 조건 추가 |
| `nexusguard/storage/memory_store.py` | 항상 거짓이던 DNS 조건 제거, 실제 수집 로그를 두 엔진 모두에 투입 |
| `nexusguard/collectors/team_collector.py` | `PASTE_ATTEMPT` 분기 추가 (파일첨부 분기보다 앞) |
| `nexusguard/schemas/event.py` | `EventAction.PASTE_ATTEMPT` 추가 |
| `source/agent/agent.py` | 확장이 보낸 `event_type` 사용 / `except:` → `except Exception:` / 붙여넣기 부가정보 통과 |
| `source/railway_server/app.py` | `raw_data` 를 `str(data)` → `json.dumps(data, ensure_ascii=False)` |
| `browser_extension/content.js` | 붙여넣기(PASTE_ATTEMPT) 감지 모듈 추가 |

`nexusguard/ui/app.py` 는 한 줄도 건드리지 않았습니다. CSS와 화면 레이아웃 그대로입니다.

## 붙여넣기 감지 — 발표용 요점

- 붙여넣기를 **막지 않습니다**. `preventDefault()` 를 호출하지 않고 관찰만 합니다.
- 붙여넣은 **내용은 전송하지 않습니다**. 나가는 값은 문자 수와 패턴 검출 개수뿐입니다.
- 감시 대상은 6개 AI 도메인뿐이고, 200자 미만은 무시합니다.
- 정상 사용자(NORMAL)는 무엇을 붙여넣어도 상태가 바뀌지 않습니다. 이미 WATCH 인 사용자만 HIGH 로 갑니다.
- 패턴 정규식에 오탐 방지를 넣었습니다. 주민번호는 월·일 유효성까지, 카드번호는 Luhn 체크섬까지 확인합니다.
  그래서 에러 로그의 타임스탬프나 주문번호는 걸리지 않습니다.

### 사생활 관련 (발표에서 짚어두면 좋은 부분)

패턴 검출은 그 자체로 내용에 대한 신호입니다. "주민번호 형태 3건"도 내용을 본 결과입니다.
실제 배포한다면 직원 사전 고지가 필요하고, 발표에서도 "내용은 절대 전송하지 않는다"는
설계 이유를 같이 설명하는 편이 좋습니다.

## 검증

프로젝트 폴더에서 아래를 실행하면 확인됩니다.

```bash
python run_demo.py                    # 인시던트 5건, 미승인 자산 7건
python test_dedup.py                  # 같은 이벤트 재투입 시 상태 전이 없음
python test_paste.py                  # NORMAL 유지 / WATCH→HIGH 승격
node   test_pattern_falsepositive.js  # 패턴 오탐 13케이스 전부 통과
```

`nexusguard.db` 를 지우지 않고 반복 실행하면 인시던트가 누적됩니다.
ID 중복은 없지만, 시연 전에는 `nexusguard.db` 를 지우고 시작하세요.

## 남은 것

1. **대시보드 실시간 반영** — `AppContext` 가 싱글톤이라 실데이터 조회가 시작 시 한 번뿐입니다.
   5초 폴링 시점에 엔진으로 넣으려면 `ui/app.py` 를 건드려야 해서 범위에서 뺐습니다.
   중복 투입 자체는 이제 엔진이 `event_id` 로 걸러주므로 안전합니다.
2. **붙여넣기 HIGH 승격 시 인시던트 미생성** — 상태 전이와 근거만 남기고 인시던트는 만들지 않습니다.
   대시보드 인시던트 목록에는 안 뜹니다.
3. **Railway 재배포** — `source/railway_server/app.py` 수정분은 Railway에 다시 배포해야 반영됩니다.
