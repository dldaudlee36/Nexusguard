# NexusGuard 실행 안내 (팀원용)

## 1. 준비 — 처음 한 번만

파이썬 3.10 이상이 필요합니다. 설치돼 있는지 확인:

```
python --version
```

없으면 https://www.python.org 에서 받아 설치하세요.
설치할 때 **"Add Python to PATH"** 체크를 꼭 하세요.

그다음 이 폴더에서:

```
pip install -r requirements.txt
```

## 2. 실행

### 대시보드 (발표에서 보여줄 화면)

```
streamlit run nexusguard/ui/app.py
```

브라우저가 자동으로 열립니다. 안 열리면 주소창에 `http://localhost:8501` 을 치세요.
끌 때는 터미널에서 `Ctrl + C`.

처음 실행하면 이메일을 물어보는데 그냥 **Enter** 치고 넘어가면 됩니다.

### 터미널 점검 (화면 없이 엔진만 확인)

```
python run_demo.py
```

정상이면 이렇게 나옵니다.

```
✅ 1. 모의 이벤트 생성 완료: 총 20건
✅ 2. 상관분석 엔진 연산 완료: 총 5개 침해 Incident 식별
✅ 3. 섀도우 IT/AI 거버넌스 엔진 검증: 총 7개 미승인 자산 식별
```

## 3. ⚠ 실행 전 반드시 — nexusguard.db 지우기

`nexusguard.db` 는 판정 결과가 쌓이는 파일입니다.
지우지 않고 여러 번 돌리면 인시던트가 5건 → 7건 → 9건으로 계속 늘어납니다.

**시연 직전에는 항상 지우고 시작하세요.**

```
del nexusguard.db
```

(맥이나 리눅스면 `rm -f nexusguard.db`)

## 4. 화면 구성

| 메뉴 | 내용 |
|---|---|
| 종합 관제 | 상태별 카운터, 인시던트 목록, 감사 이력 |
| 킬체인 분석 | 공격 경로를 네트워크 그림으로 표시 |
| AI·IT 거버넌스 | 외부 AI 자산 목록, 승인/차단 관리. 열 헤더를 누르면 정렬됩니다 |
| 중앙 서버 파이프라인 | Railway에서 받아온 실제 수집 로그 |

왼쪽 사이드바의 **실시간 시뮬레이터 1단계 → 2단계 → 3단계** 버튼이 발표의 핵심입니다.
WATCH 승격 → HIGH 확정 → 자가 치유 흐름을 순서대로 보여줍니다.

## 5. 실시간 수집을 붙이려면 (선택)

대시보드만 볼 거면 이 절은 건너뛰어도 됩니다.

### Railway 서버 연동

프로젝트 폴더에 `.env` 파일을 만들고 아래처럼 적습니다.

```
RAILWAY_API_KEY=여기에_키
```

키는 팀에서 서버를 만든 사람이 가지고 있습니다.
키를 넣은 뒤 대시보드의 **중앙 서버 파이프라인** 화면에서 `실시간 로그 수집 가동` 을 켜세요.

### 내 PC에서 로그 보내기

```
python source/agent/agent.py
```

켜두면 이 PC에서 새로 접속한 사이트가 서버로 올라갑니다.
**에이전트를 켜기 전에 방문한 사이트는 안 올라갑니다.** 시연 순서에 주의하세요.

### 크롬 확장 (파일첨부 · 붙여넣기 감지)

1. 크롬 주소창에 `chrome://extensions` 입력
2. 오른쪽 위 **개발자 모드** 켜기
3. **압축해제된 확장 프로그램을 로드합니다** 클릭
4. 이 폴더 안의 `browser_extension` 폴더 선택

확장이 감지한 내용은 위 에이전트(`agent.py`)가 켜져 있어야 서버로 전달됩니다.

## 6. 코드를 읽으려면

`주석_안내.md` 를 먼저 보세요. 전체 소스에 한국어 주석이 달려 있고,
어떤 순서로 읽으면 되는지, 어디를 왜 고쳤는지 정리돼 있습니다.

가장 중요한 파일은 `nexusguard/engine/correlation.py` 입니다. 판정 로직 전부가 거기 있습니다.

## 7. 테스트

고친 내용이 제대로 동작하는지 확인하는 스크립트입니다.

```
python test_dedup.py                    중복 이벤트 재투입 방지
python test_paste.py                    붙여넣기 판정 (NORMAL 유지 / WATCH→HIGH)
node   test_pattern_falsepositive.js    민감정보 패턴 오탐 13케이스
```

마지막 것은 Node.js 가 있어야 돌아갑니다. 없으면 건너뛰세요.

## 자주 나오는 문제

**`streamlit: 명령을 찾을 수 없습니다`**
→ `pip install -r requirements.txt` 를 안 했거나 파이썬 PATH 문제입니다.
   `python -m streamlit run nexusguard/ui/app.py` 로 시도해보세요.

**`ModuleNotFoundError: No module named 'nexusguard'`**
→ 프로젝트 폴더가 아닌 곳에서 실행한 경우입니다. `cd` 로 이 폴더에 들어간 뒤 실행하세요.

**인시던트가 5건이 아니라 더 많이 나옴**
→ `nexusguard.db` 를 안 지웠습니다. 위 3번 참고.

**중앙 서버 파이프라인 화면이 비어 있음**
→ 정상입니다. `.env` 에 키가 없거나 수집이 꺼져 있으면 비어 있습니다. 위 5번 참고.
